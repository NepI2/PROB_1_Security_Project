<Page x:Class="SecurityProject.Pages.RSA"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SecurityProject.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RSA">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Choose a file to ecrypt or use the input box to type a text to decrypt"/>
        <Button Grid.Row="1" Margin="10" Click="ChooseFile_Click">Choose File</Button>
        <TextBox Grid.Row="2" Margin="10" Text="{Binding Key}" />
        <TextBox Grid.Row="3" Margin="10" Text="{Binding DecryptedText}" />
        <TextBox Grid.Row="4" Margin="10" Text="{Binding EncryptedText}" />
        <Button Grid.Row="5" Margin="10" Click="Encrypt_Click">Encrypt</Button>
        <Button Grid.Row="6" Margin="10" Click="Decrypt_Click">Decrypt</Button>
    </Grid>
</Page>
